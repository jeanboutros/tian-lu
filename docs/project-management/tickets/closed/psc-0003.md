# Ticket: psc-0003

| Field | Value |
|-------|-------|
| Status | closed |
| Closure type | completed |
| Type | feature |
| Priority | critical |
| Created | 2026-07-30 |
| Closed | 2026-07-30 |
| Domain signals | [bash-scripting] [security] [infrastructure] [documentation] [CI/CD] |
| Specialist roster | SW, TX, DX, SX, BS, DO |
| Passport | docs/project-management/passports/psc-0003-passport.md |
| Log dir | docs/project-management/logs/tickets/psc-0003/ |
| Assigned to | code-architect |
| Replaced by |  |
| Replaces |  |
| PM decision | CLOSE |
| New tickets spawned | [] |
| Linked | ADRs: [], Conversations: [], Mistakes: [] |

## Acceptance Criteria
1. **CH-AUTH-001** — Per-environment `FLOCI_DEFAULT_ACCOUNT_ID`; account axis moves from AKID to installer config; add caller-identity precondition; update landing-zone §4.1/§4.2; run three-outcome probe; record result in gaps register
2. **CH-AUTH-002** — Rewrite §4.2 with `FLOCI_AUTH_UNSAFE_OVERRIDE` escape hatch; add bats case proving hole closed; unset `_auth_*` helpers
3. **CH-AUTH-003** — `FLOCI_SERVICES_IAM_ENABLED=true` in both branches; correct §6.2 note and SPEC-TX-006 case-3
4. **CH-AUTH-004** — Replace `sed` range delete with `awk` section-aware rewrite + atomic write; add 7 bats cases
5. **CH-AUTH-005** — `|| delete_rc=$?` for the delete under `set -e`
6. **CH-AUTH-006** — Introduce `DEV_AUTH_MODE`; call `_print_next_steps` from `dev_recreate`; add bats case
7. **CH-AUTH-007** — Atomic `.tmp+chmod+mv` for credential file; parse instead of `source`
7. **CH-AUTH-008** — Array-based `-e` overrides in guest driver (not unquoted string)
8. **CH-AUTH-009** — Retain `${arr[@]+…}` guard in `launch_driver`; decide bash-3.2 support explicitly
9. **CH-AUTH-010** — Re-derive `wait_driver` hang before specifying SPEC-TX-013
10. **CH-AUTH-011** — `DEV_AUTH_MODE` constant; gate rotation on mode; pass to installer
11. **CH-AUTH-012** — Split §6.10a–d into changelog/appendix
12. **CH-AUTH-013** — Emit `FLOCI_AUTH_MODE` to env file; surface in `dev_status`
13. **CH-AUTH-014** — Add presign-secret threat model, rotation path, and reuse-if-exists note
14. **CH-AUTH-015** — Mark §9.3 items as specified-not-verified
15. **CH-AUTH-016** — Replace "Crypto theater" with "Authenticates callers and then ignores their policies"

16. **CH-INST-001** — Retry 5xx in `verify_health`; report last code in timeout message
17. **CH-INST-002** — Per-binary AppArmor sentinel; extend twin hash set to include profile
18. **CH-INST-003** — Document or drop the four extra firewall ranges
19. **CH-INST-004** — Assert `curl` and `openssl` in Phase 1
20. **CH-INST-005** — Refresh `AGENTS.md:57` and `:64`

21. **CH-DEV-001** — `_print_next_steps` from `dev_recreate`
22. **CH-DEV-002** — `dev_env` on resume paths (Running and Stopped branches)
23. **CH-DEV-003** — Distinct return codes from `dev_disk_exists` (0/1/2)
24. **CH-DEV-004** — `DEV_DISK_MOUNT` derived from `DEV_DISK_NAME`
25. **CH-DEV-005** — Unify health budget and fallback (use `_resume_health_check` for both)
26. **CH-DEV-006** — Drop redundant `main` guard

27. **CH-TWIN-001** — Verdict on precondition failure
28. **CH-TWIN-002** — `sidecar-delta` in mandatory array
29. **CH-TWIN-003** — Replace or drop journal ordering check
30. **CH-TWIN-004** — Fix stale-sentinel cleanup path
31. **CH-TWIN-005** — Document evidence-dir split in usage and design doc
32. **CH-TWIN-006** — Resolve `--fresh`/`--keep` semantics
33. **CH-TWIN-007** — Fix `wait "${DRIVER_SHELL_PID:-}"` and `HOST_HOME` fallback

34. **CH-LZ-001** — Replace `DenyAllExceptBoundary` with three-statement form; drop non-existent group action; add G6 negative test
35. **CH-LZ-002** — Add G6 permissions-boundary evaluation gate; qualify §1.1 and §5.2/§12; record gap
36. **CH-LZ-003** — Relabel G1 to name both enforcement variables; add to §1.1 and §10.1
37. **CH-LZ-004** — G1 must fail (not skip) when probe cannot be established; main exit non-zero on any SKIP among automated gates
38. **CH-LZ-005** — Align backend region with tfvars region; unify five region literals
39. **CH-LZ-006** — Reduce §6.10b to `-backend-config=../../_common/backend.hcl` + per-stage key
40. **CH-LZ-007** — Add G3b for S3 conditional PutObject, or mark use_lockfile unverified
41. **CH-LZ-008** — Restore governance tags in `_common/providers.tf` template (Project/Environment/ManagedBy); Owner is general tag; `dev.tfvars` carries only Owner; add lint check
42. **CH-LZ-009** — Unify provider constraints to `>= 6.56.0` with NO upper bound
43. **CH-LZ-010** — Omit key from `providers.tf` to force `-backend-config` override
44. **CH-LZ-011** — Reverse `default_tags` merge order; add environment validation
45. **CH-LZ-012** — Correct mechanism in `dev.tfvars` comment and §6.10c
46. **CH-LZ-013** — Remove root `install.sh`; qualify §3 unbuilt scaffolding; add `TF_VAR_secret_key` story to §10.1; cross-link presign secret to CH-AUTH-014

47. **CH-META-001** — Record corrected mechanism for M-SW-001; fix five remaining region literals
48. **CH-META-002** — Record lesson; add standing rule for IAM Condition absent-key evaluation
49. **CH-META-003** — Record lesson; add standing rule for env-var source-line quoting

50. All 49 accepted findings resolved and verified
51. `docs/design/authentication-plan.md` updated to reflect as-implemented state (as-implemented ADR created)
52. `docs/design/landing-zone-design.md` updated with IAM delegation model as implemented
53. `scripts/preflight-floci.sh` G1 gate passes in Lima twin; G2/G3 notes updated if applicable
54. Root `install.sh` removed from repository
55. All new/updated files pass `make check` (lint + unit tests)
56. All new/updated files pass `make twin-test` (Lima digital twin integration test)

## Description
Critical remediation ticket combining **ALL 49 accepted findings** from the challenge advisory `psc-adv-0017-challenge-review` (user decisions recorded at lines 1351-1387). This ticket consolidates remediation across **19 files** spanning authentication, installer, dev twin, test harness, landing zone, and meta-corrections.

### Scope Breakdown

**A. Authentication plan (`docs/design/authentication-plan.md`) — 16 findings (CH-AUTH-001 through 016)**
- CH-AUTH-001: Per-environment `FLOCI_DEFAULT_ACCOUNT_ID`; account axis moves from AKID to installer config; caller-identity precondition; landing-zone §4.1/§4.2 updates; three-outcome probe; gaps register entry
- CH-AUTH-002: Rewrite §4.2 with `FLOCI_AUTH_UNSAFE_OVERRIDE` escape hatch; bats case proving hole closed; unset `_auth_*` helpers
- CH-AUTH-003: `FLOCI_SERVICES_IAM_ENABLED=true` in both branches; correct §6.2 note and SPEC-TX-006 case-3
- CH-AUTH-004: Replace `sed` range delete with `awk` section-aware rewrite + atomic write; **7 bats cases required** (see finding for list)
- CH-AUTH-005: `|| delete_rc=$?` for delete under `set -e`
- CH-AUTH-006: Introduce `DEV_AUTH_MODE`; call `_print_next_steps` from `dev_recreate`; bats case
- CH-AUTH-007: Atomic `.tmp+chmod+mv` for credential file; parse instead of `source`
- CH-AUTH-008: Array-based `-e` overrides in guest driver (not unquoted string)
- CH-AUTH-009: Retain `${arr[@]+…}` guard in `launch_driver`; decide bash-3.2 support explicitly
- CH-AUTH-010: Re-derive `wait_driver` hang before specifying SPEC-TX-013
- CH-AUTH-011: `DEV_AUTH_MODE` constant; gate rotation on mode; pass to installer
- CH-AUTH-012: Split §6.10a–d into changelog/appendix
- CH-AUTH-013: Emit `FLOCI_AUTH_MODE` to env file; surface in `dev_status`
- CH-AUTH-014: Add presign-secret threat model, rotation path, reuse-if-exists note
- CH-AUTH-015: Mark §9.3 items as specified-not-verified
- CH-AUTH-016: Replace "Crypto theater" with "Authenticates callers and then ignores their policies"

**B. `setup-floci.sh` — 5 findings (CH-INST-001 through 005)**
- CH-INST-001: Retry 5xx in `verify_health`; report last code in timeout message
- CH-INST-002: Per-binary AppArmor sentinel; extend twin hash set to include profile
- CH-INST-003: Document or drop the four extra firewall ranges (6500-6599, 9400-9499, 2200-2299, 9169)
- CH-INST-004: Assert `curl` and `openssl` in Phase 1
- CH-INST-005: Refresh `AGENTS.md:57` (enable_lingering) and `:64` (TLS override line)

**C. `mock-server/dev-twin.sh` — 6 findings (CH-DEV-001 through 006)**
- CH-DEV-001: `_print_next_steps` from `dev_recreate`
- CH-DEV-002: `dev_env` on resume paths (Running and Stopped branches)
- CH-DEV-003: Distinct return codes from `dev_disk_exists` (0=present, 1=absent, 2=query failed)
- CH-DEV-004: `DEV_DISK_MOUNT` derived from `DEV_DISK_NAME`
- CH-DEV-005: Unify health budget and fallback (use `_resume_health_check` for both)
- CH-DEV-006: Drop redundant `main` guard

**D. `mock-server/run-test.sh` — 7 findings (CH-TWIN-001 through 007)**
- CH-TWIN-001: Verdict on precondition failure (route through `FAIL_REASON` + `print_verdict`)
- CH-TWIN-002: `sidecar-delta` in mandatory array
- CH-TWIN-003: Replace or drop journal ordering check
- CH-TWIN-004: Fix stale-sentinel cleanup path
- CH-TWIN-005: Document evidence-dir split in usage and design doc
- CH-TWIN-006: Resolve `--fresh`/`--keep` semantics
- CH-TWIN-007: Fix `wait "${DRIVER_SHELL_PID:-}"` and `HOST_HOME` fallback

**E. Landing zone + infra — 13 findings (CH-LZ-001 through 013)**
- CH-LZ-001: Replace `DenyAllExceptBoundary` with three-statement form; drop non-existent group action; add G6 negative test
- CH-LZ-002: Add G6 permissions-boundary evaluation gate; qualify §1.1 and §5.2/§12; record gap
- CH-LZ-003: Relabel G1 to name both enforcement variables; add to §1.1 and §10.1
- CH-LZ-004: G1 must fail (not skip) when probe cannot be established; main exit non-zero on any SKIP among automated gates
- CH-LZ-005: Align backend region with tfvars region; unify five region literals
- CH-LZ-006: Reduce §6.10b to `-backend-config=../../_common/backend.hcl` + per-stage key
- CH-LZ-007: Add G3b for S3 conditional PutObject, or mark use_lockfile unverified
- CH-LZ-008: Restore governance tags in `_common/providers.tf` template (Project/Environment/ManagedBy); Owner is general tag; `dev.tfvars` carries only Owner; add lint check
- CH-LZ-009: Unify provider constraints to `>= 6.56.0` with NO upper bound
- CH-LZ-010: Omit key from `providers.tf` to force `-backend-config` override
- CH-LZ-011: Reverse `default_tags` merge order; add environment validation
- CH-LZ-012: Correct mechanism in `dev.tfvars` comment and §6.10c
- CH-LZ-013: Remove root `install.sh`; qualify §3 unbuilt scaffolding; add `TF_VAR_secret_key` story to §10.1; cross-link presign secret to CH-AUTH-014

**F. Meta-corrections — 3 findings (CH-META-001 through 003)**
- CH-META-001: Record corrected mechanism for M-SW-001; fix five remaining region literals
- CH-META-002: Record lesson; add standing rule for IAM Condition absent-key evaluation
- CH-META-003: Record lesson; add standing rule for env-var source-line quoting

### Files to Change (19 files)
1. `docs/design/authentication-plan.md`
2. `setup-floci.sh`
3. `mock-server/dev-twin.sh`
4. `mock-server/run-test.sh`
5. `mock-server/in-vm/run-in-vm.sh`
6. `docs/design/landing-zone-design.md`
7. `scripts/preflight-floci.sh`
8. `infra/live/10-management-iam/main.tf`
9. `infra/live/10-management-iam/providers.tf`
10. `infra/_common/providers.tf`
11. `infra/_common/versions.tf`
12. `infra/_common/backend.hcl.example`
13. `infra/environments/dev.tfvars`
14. `AGENTS.md`
15. `docs/design/gaps-register.md`
16. `docs/design/solution-design.md`
17. `mock-server/tests/dev_twin.bats` (CH-AUTH-004 bats cases)
18. `tests/phase5.bats` (or new `tests/preflight-g1-sigv4.bats`)
19. `install.sh` (root) — **REMOVE**

### Specialist Roster (6 specialists)
- **SW** (Software Engineer) — `setup-floci.sh` implementation, `infra/` Terraform stages, `authentication-plan.md` implementation
- **TX** (Test Engineer) — `mock-server/tests/dev_twin.bats` (7 cases for CH-AUTH-004), `tests/preflight-g1-sigv4.bats`, `make twin-test` validation
- **DX** (Docs Writer) — `authentication-plan.md`, `landing-zone-design.md`, `solution-design.md`, `gaps-register.md`, `dev-twin.sh` docs
- **SX** (Security Reviewer) — SigV4 enforcement, IAM delegation, permissions boundary, preflight G1/G6 gates, presign-secret threat model
- **BS** (Bash Specialist) — `setup-floci.sh`, `dev-twin.sh`, `run-test.sh`, `preflight-floci.sh`, `run-in-vm.sh` review
- **DO** (DevOps Specialist) — CI/CD, `make twin-test`, `scripts/preflight-floci.sh` gates, Terraform backend wiring

### Priority: critical
All 49 findings are **accepted** per user decision recorded in `psc-adv-0017-challenge-review.md` lines 1351-1387. CH-AUTH-001 and CH-LZ-001 are blockers for the landing zone deployment on Floci.

## Dependencies
- psc-0001 (Floci base installer) — must be complete
- psc-0002 (Authentication plan design) — must be complete (provides authentication-plan.md spec)
- infra/00-backend-bootstrap — must be deployed for landing zone stages